import FleetRegistersContent from './FleetRegistersContent';

export const metadata = {
  title: 'Fleet registers',
  description: 'Driver and partner registers, fuel and maintenance logs.',
};

export default function FleetRegistersPage() {
  return <FleetRegistersContent />;
}
